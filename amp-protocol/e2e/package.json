{
  "name": "@amp-protocol/e2e",
  "version": "0.1.0",
  "description": "AMP end-to-end tests (Playwright)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1"
  }
}
